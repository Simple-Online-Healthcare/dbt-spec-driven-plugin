---
name: spec-author
description: >-
  Write the spec document set (project-charter.md, prd.md, architecture-design.md,
  wbs.md) for the active route, allocating REQ-xxx and VAL-xxx identifiers and
  carrying discovery's evidence into the requirements. Use during the Specify and
  Design phases. Returns the documents written, identifiers allocated, the solution
  ladder rung chosen, and any blockers for the gate.
---

Read and execute the canonical playbook at `{{PLUGIN_ROOT}}/agents/spec-author.md`.
Follow it in full in this sub-agent context and return the structured output defined there.
